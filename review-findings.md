# RDAgents 프로젝트 검토 기록 (2026-07-12)

검토 목적: 이 프로젝트가 "실제 R&D 예산 심의 전에 기획보고서의 문제점을 보완하고 예상 질의를 파악하는 용도"로
제대로 구현되었는지 평가. 검토 당시 결론 — **실행 자체가 불가능한 상태였고, 고쳐서 돌려도 목적에 맞는 산출물이
나오지 않는 구조였음.** 아래 문제 전부를 같은 날 4단계 작업으로 해결함 (해결 상태 병기).

## A. 치명적 — 실행 불가 버그 (실행으로 재현 확인)

| # | 문제 | 위치 | 상태 |
|---|---|---|---|
| 1 | 존재하지 않는 `.get_client()` 호출 → 그래프 초기화 즉시 AttributeError. 파이프라인이 한 번도 끝까지 실행된 적 없음을 의미 | `review_graph.py` | ✅ `.get_llm()`으로 수정 |
| 2 | `bind_structured(llm, Schema)` — 필수 인자 `agent_name` 누락 → 위원장 단계에서 TypeError | review_manager / budget_coordinator / final_approver | ✅ `invoke_structured_model` 헬퍼 신설로 중앙화 + 자유 텍스트 폴백 |
| 3 | `stream_mode="values"`인데 `updates` 형식으로 파싱 → final_state에 상태 dict가 아닌 문자열이 들어감 | `propagation.py` + `review_graph.py` | ✅ 청크 자체를 final_state로 사용 |
| 4 | `bind_structured`가 None 반환 시(미지원 프로바이더) `prompt \| None`으로 체인 파괴. 폴백 헬퍼 `invoke_structured_or_freetext`는 아무도 안 씀 | `structured.py` | ✅ 2번과 함께 해결 |

## B. 로직 버그 — 실행돼도 의도와 다르게 동작

| # | 문제 | 상태 |
|---|---|---|
| 5 | Pro Reviewer가 토론 count 미증가 → 2라운드 설정에 8발언 (설계의 2배) | ✅ count 증가 추가 |
| 6 | Aggressive/Conservative Auditor도 count 미증가 → 1라운드 설정에 5발언 | ✅ count 증가 추가 |
| 7 | 최종 승인권자가 "모든 심의 이력 종합"이라면서 예산조정안+재정토론만 받음. 뒤로 갈수록 정보 소실 | ✅ 해결 — past_context와 6개 분석 보고서·전문위원회 의견 직접 전달 |
| 8 | 설정값 다수가 장식: output_language 미배선, checkpoint_enabled·results_dir·memory_log_path 미사용, backend_url 인자명 불일치로 무시, google_thinking_level 잘못된 키로 전달, past_context 항상 빈 문자열 | ✅ checkpoint 포함 나머지 설정 배선 완료. output_language만 한국어 고정 상태로 보류 |
| 9 | TradingAgents 잔재 (structured.py docstring, 미사용 import, README와 uv.lock 불일치, 기본 모델 gemini-2.5가 카탈로그에 없어 경고) | ✅ 기본 모델 gemini-3.1-pro-preview / gemini-3.5-flash로 갱신. docstring 잔재는 무해하여 보류 |
| 추가 | 검토 중 발견: `_clear_messages`가 `{"messages": []}` 반환 — add_messages 리듀서에서 no-op이라 분석가 간 메시지가 실제로 안 지워짐 | ✅ RemoveMessage + placeholder 방식으로 수정 |

## C. 목적 정합성 격차 (가장 중요했던 문제)

| # | 문제 | 상태 |
|---|---|---|
| 10 | 입력이 실제 기획보고서가 아닌 손으로 요약한 JSON. `overlap_assessment` 등 판단 결과가 입력에 미리 박혀 있어 자기 평가를 되풀이하는 순환 구조 | ✅ `--report` 원문 모드(.md/.txt) 추가. HWP는 md 변환 후 투입 |
| 11 | 예상 질의를 생성하는 노드가 아예 없음. Con Reviewer의 공격 논리(예상 질의의 원석)가 버려짐 | ✅ Question Extractor 노드 신설 — 질의 8~15개(심의기준 태그·심각도·근거·권장 답변) + 보완 권고 5~10개 |
| 12 | 중간 산출물 전부 휘발 (콘솔 3줄만 출력) | ✅ `~/.rdagents/logs/{사업ID}/{실행ID}/`에 출처·품질·비교·관측성 포함 18개 md 파일 저장 |
| 13 | 심의 기준이 일반론뿐 — 실제 심의 평가 항목 부재 | ✅ 사용자 지정 최우선 기준(**정부지원 필요성·기술개발의 중요성·시급성**)을 `review_criteria.py`에 단일 정의, 10개 에이전트 프롬프트에 공통 주입 |
| 14 | 보완 → 재심의 루프 없음 (캐시가 오히려 재심의를 방해) | ✅ 캐시 제거 + 심의 이력 메모리(재심의 시 이전 지적사항 자동 주입) |

## 검증

- `tests/test_smoke.py` (가짜 LLM, API 키 불필요): 전체 파이프라인 배선·발언 횟수(찬반 4, 3자 3)·18개 파일 생성·원문 모드·재심의·체크포인트·관측성 모두 통과.
- **미완료**: 실제 LLM 실행 (.env 미존재). API 키 설정 후 `python main.py --project quantum_computing --debug` 1회 필요.

## 의도적으로 보류한 것 (후속 후보)

- 웹 검색 연동 (유사사업·시장 수치 외부 교차 검증)
- output_language 배선 (현재 한국어 고정)
- HWP 직접 파싱 (기존 변환 도구 활용으로 대체)
- 문서 검색은 현재 키워드 빈도 기반이며 의미 기반 임베딩 검색은 미적용

관련 문서: [checklist.md](checklist.md), [context-notes.md](context-notes.md)

---

## 2026-07-12 추가 검토: 실무 적용 관점의 잔여 위험

기존 결함 수정 이후 코드를 다시 검토한 결과, 현재 구현은 멀티 에이전트 심의 흐름을 시연하는
프로토타입으로는 의미가 있으나 실제 의사결정 보조에 사용하려면 아래 항목을 추가로 해결해야 한다.

### 우선순위별 문제점

| 우선순위 | 문제 | 영향 | 개선 방향 |
|---|---|---|---|
| 높음 | 최종 승인자가 6개 전문 분석 보고서와 전문위원회 의견을 직접 받지 않음 | 중간 요약 오류가 최종 결정에서 증폭 | 최종 승인 프롬프트에 원 분석과 위원회 의견 직접 전달 |
| 높음 | 예산 판정과 금액·조건 사이의 정합성 검증 부재 | 반려인데 승인액 존재, 감액인데 증액 등 모순 허용 | Pydantic 모델 교차 필드 검증 |
| 높음 | 입력 보고서 내부 주장만 사용하고 외부 근거를 교차 검증하지 않음 | 과장·노후 자료를 모든 에이전트가 반복 신뢰 | NTIS·정책·특허·논문 등 출처 기반 검증 |
| 높음 | 현재 사업과 보고서를 모듈 전역 변수로 관리 | 병렬 실행 시 서로 다른 사업 데이터 혼입 | 실행별 컨텍스트 또는 그래프 상태로 이동 |
| 중상 | 원문 전체를 여러 도구 호출에서 반복 전달 | 토큰 비용·지연·컨텍스트 초과 | 문서 구조화, 청킹, 섹션 검색(RAG) |
| 중상 | 원문을 프롬프트에 그대로 삽입 | 문서 내부 지시문을 명령으로 오인할 가능성 | 데이터 경계와 프롬프트 인젝션 방어 규칙 |
| 중간 | 파일명 stem만으로 원문 사업을 식별하고 과거 이력을 무제한 주입 | 동명 사업 혼선 및 컨텍스트 증가 | 명시적 사업 ID·문서 해시·최근 N건 요약 |
| 중간 | checkpoint/output_language 설정이 실제 동작과 불일치 | 장애 복구 불가, 설정 신뢰 저하 | 체크포인트 배선 및 언어 설정 전달 |
| 중간 | CLI 인증 검사가 Google/OpenAI 키만 확인 | 다른 지원 프로바이더 및 로컬 모델 실행 차단 | 선택 프로바이더별 인증 검사 |
| 중간 | 최상위 예외를 출력만 하고 정상 종료 | 자동화가 실패를 성공으로 오인 | 오류별 비정상 종료 코드 반환 |
| 중간 | 정상 흐름 중심의 스모크 테스트 | 예외·동시성·실제 도구 호출 결함 미검출 | 단위·통합·실패 주입 테스트 확대 |
| 낮음~중간 | 결과 디렉터리가 초 단위 타임스탬프 | 동시 실행 결과 충돌 | UUID 또는 마이크로초 실행 ID |

### 기능 보완 로드맵

1. 예산 정합성 검증, 최종 승인자 정보 보강, 프로바이더 인증 검사, 실패 종료 코드
2. 전역 상태 제거, 실행 ID 도입, 메모리 사업 식별 및 길이 제한
3. 문서 청킹/RAG, 페이지·출처 단위 근거 추적, 입력 명령 오인 방지
4. 외부 데이터 교차 검증, 정량 평가표, 불확실성·반대 근거 표시
5. 재심의 전후 비교, 체크포인트, 토큰·비용·품질 모니터링

### 1단계 적용 기록

- ✅ `BudgetProposal`, `FinalDecision`에 금액 범위 및 판정-금액-조건 교차 검증 추가
- ✅ 최종 승인자에게 6개 전문 분석 보고서와 전문위원회 의견 직접 전달
- ✅ CLI 인증 검사를 선택한 LLM 프로바이더 기준으로 변경; Bedrock/Ollama/키 선택형 호환 서버 허용
- ✅ 인증 실패는 종료 코드 2, 실행 실패는 종료 코드 1로 반환하도록 변경
- ✅ 주요 예산 모순을 거부하는 회귀 테스트 추가
- ✅ Codex 번들 Python에 프로젝트 가상환경 의존성을 연결해 `tests/test_smoke.py` 직접 실행 통과
- ✅ 전체 `main.py`, `rdagents`, `tests` 바이트코드 컴파일 통과
- ⚠️ 개발 의존성 `pytest`, `ruff`가 설치되어 있지 않아 해당 명령 기반 검증은 수행하지 못함

### 2단계 적용 기록

- ✅ 모듈 전역 사업·원문 상태를 `ContextVar` 기반 실행 컨텍스트로 교체하여 동시 실행 간 격리
- ✅ 사업 ID의 경로 구분자·상위 경로 사용을 거부하여 입력·결과 경로 이탈 방지
- ✅ 원문 보고서는 절대 파일 경로 해시 기반 안정 ID를 사용하고 `--report-project-id` 명시 옵션 제공
- ✅ 결과 실행 디렉터리를 마이크로초 + UUID 접미사로 생성하여 동시 실행 충돌 방지
- ✅ 과거 심의 이력을 기본 최근 5건·최대 12,000자로 제한하고 환경변수로 조정 가능
- ✅ 원문 식별 안정성, 동명 파일 분리, 잘못된 사업 ID 거부, 메모리 건수 제한 테스트 추가
- ✅ 2개 병렬 스레드에서 서로 다른 원문이 교차 노출되지 않는 컨텍스트 격리 검증 통과
- ✅ 2단계 변경 후 전체 스모크·원문 재심의·전체 소스 컴파일 재통과

### 3단계 적용 기록

- ✅ 원문 전체 반복 전달을 줄 번호 보존 로컬 청크 인덱스와 관점별 키워드 검색으로 교체
- ✅ 검색 청크마다 `[출처: 파일명 L시작-L끝]` 표기를 제공하고 인용 시 해당 표기 사용 지시
- ✅ 원문을 `<untrusted_document>` 경계로 감싸고 내부 명령·역할 변경·출력 지시 무시 규칙 적용
- ✅ 입력 확장자를 `.md`/`.txt`로 제한하고 기본 최대 크기 5MB 적용
- ✅ 청크 크기·검색 개수·입력 크기 제한을 환경변수로 조정 가능
- ✅ `00_입력문서_출처.md`에 입력 파일·문서 길이·청크 수·출처 규칙 저장
- ✅ 긴 문서 후반의 관련 예산 청크 검색, 줄 번호 출처, 신뢰 경계 및 전체 회귀 테스트 통과

### 4단계 적용 기록

- ✅ `--evidence` 반복 옵션으로 NTIS 조사, 정책 문서, 특허·논문 요약 등 독립 근거 파일 다중 입력 지원
- ✅ 검색 결과에서 `[유형: 사업보고서]`와 `[유형: 외부근거]`를 구분하고 기본적으로 양쪽 근거 최소 1건 포함
- ✅ 최종 결정 이후 독립 `Quality Assessor` 노드를 추가해 결정 정당화와 품질 감사를 분리
- ✅ 정부지원 필요성 25%, 기술 중요성 20%, 시급성 15%, 기술 가능성 15%, 경제성 15%, 수행역량 10% 적용
- ✅ 모델이 제시한 종합점수를 신뢰하지 않고 기준별 점수의 가중합을 코드에서 재계산
- ✅ 기준별 점수와 별도로 근거 충분성 신뢰도를 계산하고 출처 참조를 함께 기록
- ✅ 에이전트 간 불일치, 결론에 반하는 근거, 추가 확인 자료, 외부 근거 상태를 별도 보고서로 생성
- ✅ `09_정량평가표.md`, `10_불확실성_반대근거.md` 신규 산출물 추가
- ✅ 독립 근거 양쪽 노출, 코드 가중합 계산, 16개 산출물 전체 스모크 및 컴파일 검증 통과

### 5단계 적용 기록

- ✅ 이전 심의 요약과 현재 의견·결정·보완권고를 비교하는 `Re-review Comparator` 노드 추가
- ✅ 해소된 지적, 미해소 지적, 신규 우려, 결정 변화, 예산 변화를 `11_재심의_전후비교.md`로 저장
- ✅ `checkpoint_enabled` 설정을 LangGraph SQLite 체크포인터에 실제 연결
- ✅ `--execution-id`를 체크포인트 `thread_id`로 사용하고 체크포인트 DB 경로 설정 지원
- ✅ 모든 그래프 노드의 실행시간과 응답 `usage_metadata` 토큰을 실행 상태에 누적
- ✅ 실행 ID, 프로바이더, Deep/Quick 모델, 체크포인트 상태, 노드별 시간·토큰을 `12_실행관측성.md`로 저장
- ✅ 가격표가 없는 상태에서 부정확한 비용을 산출하지 않고 '계산 안 함'으로 명시
- ✅ 신규/재심의 비교, SQLite DB 및 체크포인트 행 생성, 실행 메트릭 누적, 18개 산출물 검증 통과

### 6단계 적용 기록 (재검토 후속 조치)

재검토(2026-07-12 2차)에서 확인된 문서-구현 불일치 2건과 잔여 결함을 해소했다.

- ✅ 구조화 출력 재시도: 스키마 검증 실패 시 오류 내용을 피드백 메시지로 덧붙여 최대 2회 시도
  (`invoke_structured_model`, include_raw 경로로 파싱 오류·usage 동시 획득)
- ✅ 토큰 관측성 복구: 토론·위원장·조정관·품질평가 등 응답을 재포장하던 11개 노드가
  `make_ai_message`로 원 응답의 usage_metadata를 보존 — 실제 LLM 실행에서도 토큰 집계 가능
- ✅ 체크포인트 재개 실구현: 사용자 지정 `--execution-id`에 미완료 체크포인트가 있으면 입력 None으로
  중단 노드부터 재개 (크래시 주입 → 재개 테스트로 검증, 이전 단계 재실행 없음 확인)
- ✅ 폴백 오염 방지: 구조화 출력 실패 폴백으로 끝난 최종 결정은 심의 이력(memory)에 기록하지 않음
- ✅ `search_report(query)` 도구 신설: 분석가가 원문·외부근거에서 주제어를 직접 검색해 근거 추가 확보
- ✅ 예상 질의 품질 보증: 질의 최소 5건·권고 최소 3건 + 3대 심의기준 각 1건 이상 커버리지 검증
  (검증 실패는 재시도 피드백으로 연결)
- ✅ 품질 가중치 단일 정의: `QUALITY_WEIGHTS`(schemas.py) 한 곳에서 검증자·평가표·프롬프트가 파생
- ✅ Quality Assessor / Re-review Comparator에 한국어 출력 지시 추가
- ✅ 메모리 이력 파싱을 줄 시작 `## ` 기준 정규식으로 견고화, 체크포인트 SQLite 연결 `close()` 추가
- ✅ pytest 설치(dev extras 기존 선언 활용) — 데드 상태였던 회귀 테스트 4종 실행 편입
- ✅ 신규 테스트: 구조화 재시도 복구, 병렬 컨텍스트 격리(2단계 주장 실증), 체크포인트 크래시-재개
- ✅ 전체 pytest 9종 통과 + 전 소스 컴파일 통과
- ⚠️ 실제 LLM 실행은 여전히 미수행 (.env 미존재) — API 키 설정 후 1회 실행 필요

### 7단계 적용 기록 (원천 목표 정렬 — 점진 개조)

"예산 확보"라는 원천 목표에서 재검토한 격차 4건을 점진 개조로 해소 (git 커밋 단위 진행).

- ✅ ① 사후 검증 루프: 예상 질의를 07b_예상질의.json으로 기계 판독 가능하게 저장,
  `--verify-questions 실제질의.md --run-dir <실행>`로 의미 대조(적중/부분/미적중) 후
  가중 적중률을 코드에서 계산, `13_질의적중검증.md` + jsonl 이력 누적
- ✅ ② 토론층 교체: 실제 심의장에 없는 Pro Reviewer 제거. 심사위원 패널(공격) vs
  사업 발표자(방어) 구조로 전환 — 방어 실패·보완 인정 지점이 핵심 우려사항과 예상 질의의 원석
- ✅ ③ 모의 청문: `--hearing`으로 예상 질의 대화형 리허설. 답변 점수·강점·약점·권장 골격 평가
  + 압박 후속질의, 전 과정 기록 저장
- ✅ ④ 관문 프로파일: `--gate 부처심의|예타|예산조정|국회`로 관문별 청중·중점·질의 성향 주입.
  질의 은행: 전형 질의 패턴 시드(question_bank_seed/) + 검증된 실제 질의 자동 축적
  (~/.rdagents/question_bank/) → 회차가 쌓일수록 예측이 실전 기출에 그라운딩됨
- ✅ 전체 pytest 11종 통과. 라벨·산출물 파일명(02_심사공방_전문.md) 및 문서 갱신
- 잔여: 실제 LLM 실행(API 키), 경쟁 사업 비교 심의 모드, 시기·정책 맥락 입력(검토 의견의 4·5번)

### 8단계 적용 기록 (LLM 백엔드 3중 프로파일 + 실전 첫 완주)

- ✅ `--profile test-cerebras|test-local|prod` 도입 — 테스트는 Cerebras API(zai-glm-4.7 + gpt-oss-120b)와
  로컬 Ollama(gemma4:e4b), 실전은 Gemini 3.1 Flash
- ✅ Cerebras 프로바이더를 OpenAI 호환 레지스트리에 등록 (base_url, CEREBRAS_API_KEY, 모델명 자유 허용)
- ✅ 프롬프트 분량 예산(`prompt_char_budget`, clip_text): Cerebras 무료 티어 8K 토큰·소형 로컬 모델의
  컨텍스트 초과 중단을 방지. 입력 6구간인 질의추출기·품질평가는 구간당 예산 절반 적용
- ✅ **실제 LLM 전체 심의 첫 완주** (Cerebras, 6분석가+공방 2R+3자 검토): 조건부승인 337.5억(25% 감액),
  19개 산출물 생성, 실측 토큰 109,973 — usage 보존 수정이 실전에서 검증됨
- ⚠️ 확인된 한계: zai-glm-4.7이 예상질의·품질평가의 대형 스키마 tool call 생성에 실패
  → 자유 텍스트 폴백으로 완주 (폴백 산출물도 표 형식으로 사용 가능 수준).
  실전(prod, Gemini)에서는 구조화 성공 예상. 후속 개선 후보: json_mode 방식 구조화
- ✅ test-local(gemma4:e4b) 축소 완주 확인 (48분): 소형 모델의 구조화 실패는 전부 폴백으로 흡수,
  폴백 결정의 메모리 기록 제외 가드 정상 작동. 로컬 프로파일은 배선 검증용으로 규정
- ✅ 소형 모델이 문자열 필드에 리스트를 반환하는 패턴(FinalDecision.conditions 등)을
  field_validator로 자동 교정 — 로컬 실행에서 발견된 실패 사례를 즉시 반영
